% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fe.R
\name{fe}
\alias{fe}
\title{Fixed effects models}
\usage{
fe(formula, data, family = NULL, inference = "default", ...)
}
\arguments{
\item{formula}{A formula in the style of the \href{https://cran.r-project.org/web/packages/lme4/index.html}{\code{lme4}} package.}

\item{data}{Dataset to build model on.}

\item{family}{Family for generalized linear model. If \code{NULL}, then the function builds a linear model. Default is \code{NULL}.}

\item{inference}{Single character determining how to estimate the variance and standard errors of coefficients: can be \code{"default"} or \code{"crse"}.
If \code{inference="default"}, then the standard errors are pulled directly from the \code{lm()} (if \code{family=NULL}) or \code{glm()} (if if GLM \code{family} is specified) output.
If \code{inference="crse"}, then cluster-robust standard errors are calculated, using the \code{vcovCL()} function from the \code{sandwich} package.
Defaults to \code{inference="default"}.}

\item{...}{Additional arguments passed to \code{lm()} (if \code{family=NULL}) or \code{glm()} (if GLM \code{family} is specified).}
}
\value{
A list containing various results from the fixed effects model.
}
\description{
This function implements fixed effects models, and uses the
\href{https://cran.r-project.org/web/packages/sandwich/index.html}{\code{sandwich}} package to form
cluster-robust standard errors.

Note: Currently, the \code{mlmfe} package only allows for two-level models.
}
