% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcmlm.R
\name{bcmlm}
\alias{bcmlm}
\title{Bias-corrected multilevel models}
\usage{
bcmlm(formula, data, family = NULL, inference = "default", B = 500, ...)
}
\arguments{
\item{formula}{A formula in the style of the \href{https://cran.r-project.org/web/packages/lme4/index.html}{\code{lme4}} package.}

\item{data}{Dataset to build model on.}

\item{family}{Family for generalized linear model. If \code{NULL}, then the function builds a linear model. Default is \code{NULL}.}

\item{inference}{Single character determining how to estimate the variance and standard errors of fixed effect coefficients: can be \code{"default"}, \code{"crse"}, or \code{"boot"}.
If \code{inference="default"}, then the standard errors are pulled directly from the \code{lme4} output.
If \code{inference="crse"}, then cluster-robust standard errors are calculated. This is only allowed for linear models (when \code{family=NULL}.)
If \code{inference="boot"}, then the variance and standard errors are estimated with a cluster-bootstrap.
Defaults to \code{inference="default"}.}

\item{B}{Number of bootstrap samples when \code{inference="boot"}. Defaults to \code{B=500}.}

\item{...}{Additional arguments passed to \code{lmer()} (if \code{family=NULL}) or \code{glmer()} (if GLM \code{family} is specified) from \code{lme4}.}
}
\value{
A list containing various results from the bias-corrected multilevel model.
}
\description{
This function implements bias-corrected multilevel models
as described in \href{https://arxiv.org/abs/2411.01723}{Bai et al. (2026)} and \href{https://www.cambridge.org/core/journals/political-analysis/article/understanding-choosing-and-unifying-multilevel-and-fixed-effect-approaches/8101D49CFD3B129F5753FC878F416980}{Hazlett and Wainstein (2022)},
using the \href{https://cran.r-project.org/web/packages/lme4/index.html}{\code{lme4}} package.

Note: Currently, the \code{mlmfe} package only allows for two-level models.
}
